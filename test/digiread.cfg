process TEST = {
       source = PoolSource {
                untracked string fileName = "digi.root"
                untracked int32 maxEvents = 5
	        }

	module analyzer = DigiAnalyzer {
	        	
		          } 

	path p = {analyzer}
}
