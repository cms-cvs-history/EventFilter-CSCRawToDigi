process ANAL = {
       source = PoolSource {
                untracked vstring fileNames = {"file:digi.root"}
                untracked int32 maxEvents = 5
	        }

	module analyzer = DigiAnalyzer {

	}	

	path p = {analyzer}
}
